# /mcp

Rooted's own MCP server (FastMCP), wrapping the SBR API as three curated, well-named tools:
verify_asset, recover_manifest, query_transparency_log. OAuth 2.1, not an auto-converted full REST
surface. Lets AI agents verify provenance conversationally.
