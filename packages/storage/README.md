# /packages/storage

Backblaze B2 access via b2sdk (content-addressable layout, Object Lock compliance retention) and the
Genblaze ObjectStorageSink wiring. Dev bucket (no retention) for iteration; locked bucket for the
final verified run only.
