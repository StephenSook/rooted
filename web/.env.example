# Base URL of the FastAPI SBR backend the typed client talks to.
# Local dev defaults to http://localhost:8000 if unset; set this for deployed environments.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
